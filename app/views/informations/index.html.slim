.title
  .title-content
    = "L'actu c'est ici !"

.new
  .new-content
    = form_for @information, url: {action: "create"}, html: {class: "nifty_form"} do |f|
      .new-content-label
        = f.label "information:"
      .new-content-field
        = f.text_area :content
      .new-content-tag
        = f.submit "   "
        = "je souhaite diffuser cet info"

.index
  - @informations.each do |information|
    .index-content
      .index-content-middle
        = information.content
      .index-content-bottom
        = link_to "réagis", information_path(information.id)
